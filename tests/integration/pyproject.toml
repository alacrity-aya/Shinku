[project]
name = "test"
version = "0.1.0"
description = "Integration tests for dns-cache"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
full = [
    "pyroute2>=0.9.5",
    "scapy>=2.7.0",
    "pytest>=8.0",
]
